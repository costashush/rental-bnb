# Clone this project then follow the instructions : )
## Backend 
In the backend directory, you can run:

### `npm install`

Then 

### `npm run dev`


## Frontend

In the client directory, you can run:

### `npm install`

Then 

### `npm start`



